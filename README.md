i stopped using my own aur helper and use `yay` now instead  
i just hated using bash; god do i hate that language

for most basic things `nicaur` works though, mostly  
it won't look for updates though, so you gotta manually do that
